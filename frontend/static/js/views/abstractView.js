export default class {
  constructor(params) {
    this.params = params;

  }
  //methods
  // set title in browser tab
  setTitle(title) {
    document.title = title;
  }

  async getHtml() {
    return "";
  }
}